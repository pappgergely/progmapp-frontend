import { FilterQuizPipe } from './filter-quiz.pipe';

describe('FilterQuizPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterQuizPipe();
    expect(pipe).toBeTruthy();
  });
});
