import { FilterClassQuizPipe } from './filter-class-quiz.pipe';

describe('FilterClassQuizPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterClassQuizPipe();
    expect(pipe).toBeTruthy();
  });
});
