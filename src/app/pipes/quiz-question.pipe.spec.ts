import { QuizQuestionPipe } from './quiz-question.pipe';

describe('QuizQuestionPipe', () => {
  it('create an instance', () => {
    const pipe = new QuizQuestionPipe();
    expect(pipe).toBeTruthy();
  });
});
