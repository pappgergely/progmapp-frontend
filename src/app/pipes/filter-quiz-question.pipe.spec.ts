import { FilterQuizQuestionPipe } from './filter-quiz-question.pipe';

describe('FilterQuizQuestionPipe', () => {
  it('create an instance', () => {
    const pipe = new FilterQuizQuestionPipe();
    expect(pipe).toBeTruthy();
  });
});
